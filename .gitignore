node_modules/
dist/
build/
.vite/

.env
.env.local
.env.*.local
*.pem
*.key

npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

.vscode/
.idea/

postgres-data/
*.sqlite

# Scratch file used to hand over instructions. Kept on disk, never committed.
do this.txt
do this

# Vercel CLI scratch, if it is ever used locally.
.vercel/

# Receipt photos used to measure the OCR preprocessing. Somebody's real receipts
# with real shop names and real amounts on them: they stay on the machine that
# took them. The README explaining the naming is committed; nothing else in there
# ever is.
receipt-fixtures/*
!receipt-fixtures/README.md
